This is a Tanstack router template with integrated tanstack query, and tailwind for styling

### Make sure to upgrade your packages to lates version by running 
 - yarn: yarn upgrade --latest
 - pnpm: pnpm up --latest
 - npm: npx npm-check-updates -u
